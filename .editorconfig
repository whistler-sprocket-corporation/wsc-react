# Editor configuration, see https://editorconfig.org
# Editor Config is the final say in formatting, these settings are copied in the prettier config.

root = true

[*]
end_of_line = lf
max_line_length = 80
charset = utf-8
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true